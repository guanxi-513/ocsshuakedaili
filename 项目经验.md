# OCS AI 答题助手 - 项目经验总结

## 项目简介

将 Python 后端对接 OCS 刷题平台，支持三个后端：DeepSeek 网页版反代、Ollama 本地模型、豆包网页版反代。通过一键启动脚本引导用户完成环境检测、登录、后端选择等流程。

***

## 一、高频问题与解决方案

### 1. 批处理文件编码问题

**现象**：双击 `start.bat` 后窗口闪退，显示乱码如 `锘緻echo off`、`'瓧' 不是内部或外部命令`。

**原因**：

- 文件保存为 UTF-8 BOM 编码，但 Windows cmd.exe 默认以 ANSI(GBK) 解析批处理文件

- UTF-8 BOM 的三个字节 `EF BB BF` 在 GBK 编码下被解析为汉字"锘"

- 中文字符以 UTF-8 编码存储，被 GBK 解码后全部变成乱码

- 乱码导致 `if`、`goto`、`set` 等命令解析失败，脚本直接崩溃

**解决**：批处理文件必须以 **ANSI(GBK) 编码**保存，不要在中文 Windows 上用 UTF-8 BOM 写 `.bat` 文件。

**经验**：使用 PowerShell 的 `Out-File -Encoding Default` 生成批处理文件，或在纯英文环境下编写避免此问题。

***

### 2. Python 路径检测触发微软商店弹窗

**现象**：脚本执行 `python --version` 检测时，Windows 10/11 弹出"打开 Microsoft Store 获取 Python"的对话框，导致脚本卡死。

**原因**：当 Python 未加入系统 PATH 时，Windows 的"应用执行别名"功能会将 `python` 命令重定向到微软商店。

**解决**：**不要直接运行** **`python`** **命令检测**，改用 `if exist` 直接检查常见安装路径下的 `python.exe` 文件是否存在：

```batch
if exist "C:\Users\xxx\AppData\Local\Programs\Python\Python313\python.exe" (
    set PYTHON=...\python.exe
)
```

***

### 3. 批处理中 `if/else` 块的解析问题

**现象**：脚本执行到 `if/else` 块时卡住或闪退，日志显示 `if` 条件判断后的 `echo` 命令未执行。

**原因**：批处理中嵌套的 `if/else` 块（尤其是 `if ... ( ... ) else ( ... )` 语法）在复杂场景下解析不稳定，特别是括号内包含其他 `if` 语句时。

**解决**：改用 `if/goto` 模式替代 `if/else`：

```batch
REM 不推荐：
if "x"=="y" (
    echo a
) else (
    echo b
)

REM 推荐：
if "x"=="y" goto :LABEL_A
echo b
goto :DONE
:LABEL_A
echo a
:DONE
```

***

### 4. 批处理中执行 Python `-c` 命令失败

**现象**：`"!PYTHON!" -c "import playwright" >nul 2>&1` 命令执行时脚本卡死，日志停止在该行。

**原因**：在批处理环境中，通过 `-c` 参数传递带引号的 Python 代码可能与批处理的引号解析产生冲突，特别是当 Python 路径包含空格时。

**解决**：创建独立的 Python 脚本文件，通过文件路径调用：

```batch
REM 不推荐：
"!PYTHON!" -c "import playwright" >nul 2>&1

REM 推荐（创建 _check_deps.py）：
"!PYTHON!" "%~dp0_check_deps.py" >"%TEMP%\ocs_deps_check.txt" 2>&1
```

***

### 5. `chcp 65001` 与 ANSI 编码文件的冲突

**现象**：设置 `chcp 65001` 后，脚本中的中文反而显示为乱码。

**原因**：`chcp 65001` 将控制台代码页切换为 UTF-8，但文件本身是 ANSI(GBK) 编码。`echo` 输出的 GBK 字节被控制台当作 UTF-8 解码，导致乱码。

**解决**：ANSI 编码的批处理文件不需要 `chcp 65001`，默认 GBK 编码即可正常显示中文。如需 Python 输出 UTF-8，在 Python 内部处理编码。

***

### 6. 重定向输出截断问题

**现象**：日志文件只有部分内容，关键步骤的日志丢失。

**原因**：`>nul 2>&1` 将 stdout 和 stderr 都丢弃了，导致无法看到错误信息。同时，日志文件写入可能被缓冲。

**解决**：调试阶段不要丢弃输出，改用追加到日志文件：

```batch
REM 调试阶段使用：
"!PYTHON!" "%~dp0_check_deps.py" >>"%LOG_FILE%" 2>&1

REM 正式版本再抑制输出：
"!PYTHON!" "%~dp0_check_deps.py" >nul 2>&1
```

***

## 二、架构难点

### 1. Playwright 多线程问题

**问题**：`ThreadingHTTPServer` 在主线处理请求，Playwright 也在主线程运行时，出现 `cannot switch to a different thread` 错误。

**解决**：采用**专用工作线程 + 消息队列**模式。所有 Playwright 操作在独立的后台线程中执行，主线程通过队列传递请求和接收结果。

### 2. 网页版 AI 的会话保持

**问题**：DeepSeek/豆包网页版需要保持登录状态，刷新页面会触发人机验证。

**解决**：

- 使用 `--user-data-dir` 保存浏览器配置文件，复用登录状态

- 避免不必要的页面刷新，通过选择器直接定位聊天内容

- 使用长连接和会话 ID 复用已有对话

### 3. 多后端切换架构

**设计**：通过 `app.py --mode` 参数切换后端，每种后端独立实现但遵循统一的接口：

```
app.py (HTTP 服务器)
  ├── --mode deepseek  → deepseek_backend.py (Playwright 操作 DeepSeek 网页)
  ├── --mode ollama    → ollama_backend.py (HTTP 请求本地 Ollama API)
  └── --mode doubao    → doubao_backend.py (Playwright 操作豆包网页)
```

***

## 三、DeepSeek 网页版答案提取与匹配（踩坑最多）

DeepSeek 网页版自动化是这个项目中最难稳定的部分，先后出现"发不出去""超时""答案错乱"三类问题，逐一拆解：

### 1. 问题：题目发不出去 / 偶发超时

**现象**：第一题正常，第二题开始卡住，91 秒后报 `DeepSeek 回复超时`。91 秒恰好是发送时刻 + 90 秒轮询上限，说明**问题根本没发出去**。

**原因**：第一题发送后，DeepSeek 界面状态发生改变（输入框行为、焦点等），代码里 `fill` / 回车按下未真正生效，问题没有发送。

**解决**：

- 发送后主动**校验**：检查页面文本是否包含题目片段，未包含则重新发送（最多重试 2 次）
- `prefix = prompt.split('\n')[0][:10]` 取题干前 10 个字符做校验锚点，避免整段 prompt 因排版差异匹配失败

### 2. 问题：能拿到回复，但内容是整段"回显"

**现象**：日志里 `AI 原始回答` 是一大段 `选项： A. ... B. ... C. ...` 的汉字，真正的答案字母混在里面，匹配逻辑被回显干扰而选错。

**原因**：背端用"取 prompt 之后的所有文本"做答案提取，只要 DeepSeek 回显了题目和选项，这些汉字就全被抓进来了。

**解决**：**不要提取整段文本，改为只挑"纯字母行"**。DeepSeek 按提示要求只输出字母时，页面中新增的行就是 `A`、`ACD` 这类纯大写字母行。用正则过滤：

```javascript
// 从页面所有行中，挑出"纯大写字母"的行作为答案
const lines = document.body.innerText.split('\n').map(l => l.trim()).filter(l => l.length > 0);
const candidates = [];
for (const line of lines) {
    if (/^[A-H]{1,8}$/.test(line)) candidates.push(line.toUpperCase());
}
return candidates.length ? candidates[candidates.length - 1] : null;
```

回显行 `A. 中文`、系统 UI 词都不会匹配 `^[A-H]{1,8}$`，从而被自然排除。

### 3. 问题：AI 回显题目导致匹配错乱

**现象**：`AI 原始回答` 正确包含了 `C. 日莲宗`，但程序返回了选项 A 的"禅宗"。

**原因**：匹配逻辑里，回显的选项行 `A. 禅宗`、`C. 日莲宗` 都被当成候选，全文本包含匹配时 `answer in opt` 命中第一个选项就返回。

**解决**：**在提示词里强制 AI 只输出字母，禁止回显题目/选项/解释**。这是从源头根治，让 AI 输出变成干净的 `A` 或 `ACD`：

```python
if qtype == "multiple":
    prompt_parts.append("\n\n这是多选题，请只输出所有正确选项的字母，多个字母直接连写（如 AC 或 ACD），不要输出题目、选项内容或任何解释。")
elif qtype == "completion":
    prompt_parts.append("\n\n请直接给出填空答案，不要解释。")
else:
    prompt_parts.append("\n\n请只输出正确选项的字母（如 A），不要重复题目、选项内容或任何解释。")
```

### 4. 问题：匹配结果不稳定

**现象**：有时返回正确选项，有时返回错误的"第一个匹配到的选项"。

**原因**：

- `match_answer` 原来的"逐行/包含"匹配拿不到干净的字母时，会在最后用 `_match_single(ai_answer, option_list)` 做全文本包含匹配，回显内容会命中错误的选项
- 单选多选未区分，多选题在逐行匹配里提前 `return`，只拿到第一个字母

**解决**：

- `match_answer` 增加 `qtype` 参数，**多选题跳过逐行 early-return**，统一走字母提取
- **单选优先取"最后一个带编号的答案行"**（真正的答案通常在末尾），兜底再走逐行/字母匹配

### 5. 关键经验总结

| 现象                     | 根因                       | 解法                                     |
| ------------------------ | -------------------------- | ---------------------------------------- |
| 题目发不出去 / 偶发超时     | 发送后未校验，界面状态变化  | 发送后校验命中原题片段，失败重试               |
| 回复是整段汉字回显          | 提取了"prompt 之后全部文本" | 只提取纯字母行 `^[A-H]{1,8}$`              |
| 匹配到错误选项             | 回显干扰 + 未区分题型的匹配  | 提示词强制只输出字母 + match_answer 按题型分治  |
| 多选题只识别第一个答案      | 逐行匹配提前 return         | 多选跳过逐行，统一提取全部字母，`===` 分隔        |

**核心教训**：答案提取和匹配都应该面向"AI 被要求输出的干净格式"，而不是尝试从 AI 的任意自由文本里猜。**先约束 AI 的输出，再让解析变简单**，比在解析端堆各种兼容分支可靠得多。

***

## 四、批处理脚本开发避坑指南

| 问题        | 错误做法               | 正确做法                                        |
| --------- | ------------------ | ------------------------------------------- |
| 编码        | UTF-8 BOM          | ANSI(GBK)                                   |
| Python 检测 | `python --version` | `if exist` 检查文件                             |
| 条件分支      | `if/else` 嵌套块      | `if/goto` 模式                                |
| 执行 Python | `-c "code"`        | 独立 `.py` 文件                                 |
| 变量延迟      | 全局 `%var%`         | `setlocal enabledelayedexpansion` + `!var!` |
| 特殊字符      | `()` 使用全角          | 一律使用半角 `()`                                 |
| 换行符       | LF                 | CRLF (Windows 标准)                           |
| 路径引号      | 不加引号               | 含空格的路径必须加 `"`                               |
| 错误输出      | `>nul 2>&1` 丢弃     | 调试时写入日志文件                                   |

***

## 五、OCS 题库配置对接

### 1. AnswererWrapper 配置格式

OCS 的题库配置通过 `AnswererWrapper` 数组定义，每个配置项包含以下字段：

| 参数 | 必填 | 说明 |
|------|:----:|------|
| `name` | 是 | 题库名称，显示在 OCS 界面 |
| `url` | 是 | 请求地址，支持 `${title}`、`${type}`、`${options}` 占位符 |
| `method` | 否 | 默认 `get`，请求方法 |
| `contentType` | 否 | 默认 `json`，定义 handler 收到的数据类型 |
| `type` | 否 | 默认 `fetch`，`GM_xmlhttpRequest` 可跨域 |
| `handler` | 是 | 处理响应的函数，返回 `[题目, 答案]` |
| `homepage` | 否 | 题库网址 |
| `headers` | 否 | 自定义请求头 |
| `data` | 否 | 额外参数，GET 追加到 URL，POST 作为请求体 |

**标准配置示例：**

```json
[
  {
    "contentType": "json",
    "handler": "return (res)=> [undefined, res[1]]",
    "homepage": "http://10.91.29.234:8080",
    "method": "get",
    "name": "AI 自动答题",
    "type": "GM_xmlhttpRequest",
    "url": "http://10.91.29.234:8080/search?title=${title}&type=${type}&options=${options}"
  }
]
```

### 2. Handler 响应格式

handler 是一个字符串函数体，接收 `res`（接口返回的 JSON 数据），返回：

- `[题目, 答案]` — 单个结果，`题目` 为 `undefined` 时使用原题
- `[[题目1, 答案1], [题目2, 答案2]]` — 多个结果
- `undefined` — 未搜索到

**常见 handler 模式：**

```javascript
// 接口返回 [null, "answer"]
return (res)=> [undefined, res[1]]

// 接口返回 {code: 1, answer: "xxx"}
return (res)=> res.code === 1 ? [undefined, res.answer] : undefined

// 接口返回 {code: 1, results: [{question, answer}]}
return (res)=> res.code === 1 ? res.results.map(r=>([r.question, r.answer])) : undefined
```

### 3. 跨域问题

- **`fetch` 类型**（默认）— 受浏览器同源策略限制，跨域请求会被拦截
- **`GM_xmlhttpRequest` 类型** — 使用油猴 API，可跨域，但需要 `@connect` 权限
- 首次使用 `GM_xmlhttpRequest` 时，油猴会弹出"允许所有域名"的提示，点**始终允许**即可
- 如果服务在主机、OCS 在虚拟机，必须用 `GM_xmlhttpRequest` + 主机 IP 地址

### 4. 配置加载方式

- **URL 自动加载**：填配置 URL，OCS 自动 fetch 获取配置（跨域可能失败）
- **手动填写**：逐项输入配置字段（推荐，不受跨域限制）
- 配置接口 `/config` 返回的 URL 基于请求的 Host 头动态生成，适配不同网络环境

---

## 六、跨网络部署

### 1. 主机 + 虚拟机架构

![架构图](https://trae-api-cn.mchost.guru/api/ide/v1/text_to_image?prompt=Architecture+diagram+showing+a+host+machine+running+Python+backend+service+on+port+8080%2C+connected+to+a+VMware+VM+running+Chrome+with+OCS+plugin+via+bridged+network%2C+host+IP+10.91.29.234+and+VM+IP+10.91.29.203&image_size=landscape_4_3)

### 2. VMware 网络模式选择

| 模式 | VM 独立 IP | 主机 ↔ VM 互通 | 需路由器 |
|------|:-:|:-:|:-:|
| **桥接 (Bridged)** | 同一局域网独立 IP | 能 | 需要 |
| NAT | 虚拟子网 IP | 需端口转发 | 不需要 |
| 仅主机 | 虚拟子网 IP | 仅主机能访问 | 不需要 |

- 手机热点场景：桥接模式，VM 获取热点网段 IP
- 注意：部分手机热点开启 AP 隔离会导致设备间无法互通

### 3. 防火墙配置

主机需放行对应端口，否则 VM 无法访问主机服务：

```
netsh advfirewall firewall add rule name="OCS" dir=in action=allow protocol=TCP localport=8080
```

---

## 七、Playwright 浏览器自动化

### 1. 配置文件锁冲突

**问题**：`launch_persistent_context` 使用 `user_data_dir` 时，如果该目录已被其他浏览器进程占用，会报错：

```
BrowserType.launch_persistent_context: Target page, context or browser has been closed
```

**原因**：Edge 浏览器关闭窗口后，后台进程可能仍在运行，锁定配置文件目录。

**解决**：
- 启动 Playwright 前杀掉所有 Edge 后台进程：`taskkill /f /im msedge.exe`
- 或用独立的配置文件目录，与登录引导使用的目录分开

### 2. 多线程架构

```python
# 专用工作线程 + 消息队列
_REQUEST_QUEUE = queue.Queue()
_RESPONSE_QUEUE = queue.Queue()
_WORKER_READY = threading.Event()

def _worker():
    # Playwright 操作都在此线程执行
    with sync_playwright() as p:
        context = p.chromium.launch_persistent_context(...)
        # 处理请求...

_thread = threading.Thread(target=_worker, daemon=True)
_thread.start()
```

---

## 八、调试技巧

1. **日志先行**：在脚本的每一步都写入日志文件，这是定位闪退问题的最有效手段
2. **逐步验证**：先让脚本跑通最简单的路径（手动输入），再逐步添加自动检测
3. **备份稳定版本**：每次重大改动前备份当前可工作的版本（`start.bat.backup`）
4. **用 PowerShell 直接测试**：批处理执行有问题时，用 PowerShell 直接调 Python 确认问题在哪一层

