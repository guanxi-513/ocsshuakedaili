# OCS AI 答题助手 - 项目经验总结

## 项目简介

将 Python 后端对接 OCS 刷题平台，支持三个后端：DeepSeek 网页版反代、Ollama 本地模型、豆包网页版反代。通过一键启动脚本引导用户完成环境检测、登录、后端选择等流程。

---

## 一、高频问题与解决方案

### 1. 批处理文件编码问题

**现象**：双击 `start.bat` 后窗口闪退，显示乱码如 `锘緻echo off`、`'瓧' 不是内部或外部命令`。

**原因**：
- 文件保存为 UTF-8 BOM 编码，但 Windows cmd.exe 默认以 ANSI(GBK) 解析批处理文件
- UTF-8 BOM 的三个字节 `EF BB BF` 在 GBK 编码下被解析为汉字"锘"
- 中文字符以 UTF-8 编码存储，被 GBK 解码后全部变成乱码
- 乱码导致 `if`、`goto`、`set` 等命令解析失败，脚本直接崩溃

**解决**：批处理文件必须以 **ANSI(GBK) 编码**保存，不要在中文 Windows 上用 UTF-8 BOM 写 `.bat` 文件。

**经验**：使用 PowerShell 的 `Out-File -Encoding Default` 生成批处理文件，或在纯英文环境下编写避免此问题。

---

### 2. Python 路径检测触发微软商店弹窗

**现象**：脚本执行 `python --version` 检测时，Windows 10/11 弹出"打开 Microsoft Store 获取 Python"的对话框，导致脚本卡死。

**原因**：当 Python 未加入系统 PATH 时，Windows 的"应用执行别名"功能会将 `python` 命令重定向到微软商店。

**解决**：**不要直接运行 `python` 命令检测**，改用 `if exist` 直接检查常见安装路径下的 `python.exe` 文件是否存在：

```batch
if exist "C:\Users\xxx\AppData\Local\Programs\Python\Python313\python.exe" (
    set PYTHON=...\python.exe
)
```

---

### 3. 批处理中 `if/else` 块的解析问题

**现象**：脚本执行到 `if/else` 块时卡住或闪退，日志显示 `if` 条件判断后的 `echo` 命令未执行。

**原因**：批处理中嵌套的 `if/else` 块（尤其是 `if ... ( ... ) else ( ... )` 语法）在复杂场景下解析不稳定，特别是括号内包含其他 `if` 语句时。

**解决**：改用 `if/goto` 模式替代 `if/else`：

```batch
REM 不推荐：
if "x"=="y" (
    echo a
) else (
    echo b
)

REM 推荐：
if "x"=="y" goto :LABEL_A
echo b
goto :DONE
:LABEL_A
echo a
:DONE
```

---

### 4. 批处理中执行 Python `-c` 命令失败

**现象**：`"!PYTHON!" -c "import playwright" >nul 2>&1` 命令执行时脚本卡死，日志停止在该行。

**原因**：在批处理环境中，通过 `-c` 参数传递带引号的 Python 代码可能与批处理的引号解析产生冲突，特别是当 Python 路径包含空格时。

**解决**：创建独立的 Python 脚本文件，通过文件路径调用：

```batch
REM 不推荐：
"!PYTHON!" -c "import playwright" >nul 2>&1

REM 推荐（创建 _check_deps.py）：
"!PYTHON!" "%~dp0_check_deps.py" >"%TEMP%\ocs_deps_check.txt" 2>&1
```

---

### 5. `chcp 65001` 与 ANSI 编码文件的冲突

**现象**：设置 `chcp 65001` 后，脚本中的中文反而显示为乱码。

**原因**：`chcp 65001` 将控制台代码页切换为 UTF-8，但文件本身是 ANSI(GBK) 编码。`echo` 输出的 GBK 字节被控制台当作 UTF-8 解码，导致乱码。

**解决**：ANSI 编码的批处理文件不需要 `chcp 65001`，默认 GBK 编码即可正常显示中文。如需 Python 输出 UTF-8，在 Python 内部处理编码。

---

### 6. 重定向输出截断问题

**现象**：日志文件只有部分内容，关键步骤的日志丢失。

**原因**：`>nul 2>&1` 将 stdout 和 stderr 都丢弃了，导致无法看到错误信息。同时，日志文件写入可能被缓冲。

**解决**：调试阶段不要丢弃输出，改用追加到日志文件：

```batch
REM 调试阶段使用：
"!PYTHON!" "%~dp0_check_deps.py" >>"%LOG_FILE%" 2>&1

REM 正式版本再抑制输出：
"!PYTHON!" "%~dp0_check_deps.py" >nul 2>&1
```

---

## 二、架构难点

### 1. Playwright 多线程问题

**问题**：`ThreadingHTTPServer` 在主线处理请求，Playwright 也在主线程运行时，出现 `cannot switch to a different thread` 错误。

**解决**：采用**专用工作线程 + 消息队列**模式。所有 Playwright 操作在独立的后台线程中执行，主线程通过队列传递请求和接收结果。

### 2. 网页版 AI 的会话保持

**问题**：DeepSeek/豆包网页版需要保持登录状态，刷新页面会触发人机验证。

**解决**：
- 使用 `--user-data-dir` 保存浏览器配置文件，复用登录状态
- 避免不必要的页面刷新，通过选择器直接定位聊天内容
- 使用长连接和会话 ID 复用已有对话

### 3. 多后端切换架构

**设计**：通过 `app.py --mode` 参数切换后端，每种后端独立实现但遵循统一的接口：

```
app.py (HTTP 服务器)
  ├── --mode deepseek  → deepseek_backend.py (Playwright 操作 DeepSeek 网页)
  ├── --mode ollama    → ollama_backend.py (HTTP 请求本地 Ollama API)
  └── --mode doubao    → doubao_backend.py (Playwright 操作豆包网页)
```

---

## 三、批处理脚本开发避坑指南

| 问题 | 错误做法 | 正确做法 |
|------|---------|---------|
| 编码 | UTF-8 BOM | ANSI(GBK) |
| Python 检测 | `python --version` | `if exist` 检查文件 |
| 条件分支 | `if/else` 嵌套块 | `if/goto` 模式 |
| 执行 Python | `-c "code"` | 独立 `.py` 文件 |
| 变量延迟 | 全局 `%var%` | `setlocal enabledelayedexpansion` + `!var!` |
| 特殊字符 | `()` 使用全角 | 一律使用半角 `()` |
| 换行符 | LF | CRLF (Windows 标准) |
| 路径引号 | 不加引号 | 含空格的路径必须加 `"` |
| 错误输出 | `>nul 2>&1` 丢弃 | 调试时写入日志文件 |

---

## 四、调试技巧

1. **日志先行**：在脚本的每一步都写入日志文件，这是定位闪退问题的最有效手段
2. **逐步验证**：先让脚本跑通最简单的路径（手动输入），再逐步添加自动检测
3. **备份稳定版本**：每次重大改动前备份当前可工作的版本（`start.bat.backup`）
4. **用 PowerShell 直接测试**：批处理执行有问题时，用 PowerShell 直接调 Python 确认问题在哪一层